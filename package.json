{
  "name": "t-viewer",
  "version": "1.0.0",
  "description": "T-Viewer — LSP deco log viewer for Android",
  "private": true,
  "scripts": {
    "build": "echo 'No build step — static HTML'"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/share": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
